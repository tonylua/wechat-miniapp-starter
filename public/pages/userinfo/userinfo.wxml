<view class="warn_img">{{ code }}</view>

<text>{{ message }}</text>

<view class="buttons">
  <button 
    open-type="getUserInfo" 
    type="primary" 
    bindgetuserinfo="onUserInfo">
    {{ button }}
    </button>
</view>